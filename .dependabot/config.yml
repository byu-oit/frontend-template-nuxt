version: 1
update_configs:
  - package_manager: "javascript"
    directory: "/"
    update_schedule: "live"
    default_reviewers:
      - blakemorgan
    ignored_updates:
    # The following is required until nuxt 2.9.x is released
    - match:
        dependency_name: 'css-loader'
        version_requirement: '^3.0.0'
